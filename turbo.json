{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "deep-clean": {
      "dependsOn": []
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["src/**", "tests/**"]
    }
  }
}
